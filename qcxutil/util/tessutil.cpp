#include "tessutil.h"
#include "Include/tesselator.h"

namespace qcxutil
{
    void tessPolygon(const std::vector<std::vector<trimesh::vec3>>& polygons, std::vector<trimesh::vec3>& vertexs)
    {

    }
}